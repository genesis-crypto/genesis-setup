name: Extract Content

on: 
  issues:
    types: [opened, reopened]

jobs:
  extract-content:
    runs-on: ubuntu-latest
    steps:
    - name: Extracting content
      run: |
        content=$(echo "${{ github.event.issue.body }}" | grep -E '(^### \[Repository\]$|^### \[Template\]$)' -A1 | tail -1)
        repository=$(echo "$content" | grep -E '^### \[Repository\]$' -A1 | tail -1)
        template=$(echo "$content" | grep -E '^### \[Template\]$' -A1 | tail -1)
        echo "Repository: $repository"
        echo "Template: $template"
        echo "::set-output name=repository::$repository"
        echo "::set-output name=template::$template"

    - name: Save output to cache
      id: save-on-cache
      uses: actions/cache@v2
      with:
        path: outputs
        key: ${{ runner.os }}-extract-content-${{ hashFiles('outputs/repository outputs/template') }}
        restore-keys: |
          ${{ runner.os }}-extract-content-